---
title: Standards
description: Your engineering standards, written once and enforced everywhere.
template: splash
hero:
  tagline: Your engineering standards, written once and enforced everywhere.
  actions:
    - text: JSON Schemas
      link: /schemas/
      icon: document
      variant: primary
---

Standards turns your engineering rules into code an agent can enforce on
every pull request.

The full documentation is being written. The JSON Schema files for the
configuration and lock file formats are already served from their canonical
URLs on the [JSON Schemas](./schemas/) page.